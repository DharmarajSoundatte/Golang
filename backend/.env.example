# ── Server ────────────────────────────────────────────────────────────────────
PORT=8080
APP_ENV=development

# ── Database ──────────────────────────────────────────────────────────────────
# Option A: full Aiven (or any cloud) connection URL  ← recommended
DATABASE_URL=postgres://avnadmin:YOUR_PASSWORD@golang-egan-be79.i.aivencloud.com:17722/dharmraaj?sslmode=require
SSL_ROOT_CERT=ca.pem

# Option B: individual vars (used only when DATABASE_URL is not set)
#DB_HOST=localhost
#DB_PORT=5432
#DB_USER=postgres
#DB_PASSWORD=postgres
#DB_NAME=golang_poc
#DB_SSL_MODE=disable

# ── JWT ───────────────────────────────────────────────────────────────────────
JWT_SECRET=super-secret-key-change-in-production
JWT_EXPIRE_HOURS=24

# ── CORS ──────────────────────────────────────────────────────────────────────
ALLOWED_ORIGINS=http://localhost:5173
